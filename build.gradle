plugins {
    id 'java'
}

group 'top.edebe.util'
version '5.0.0'

repositories {
    mavenCentral()
}

dependencies {
    compileOnly "org.jetbrains:annotations:${annotations_version}"
    compileOnly "org.projectlombok:lombok:${lombok_version}"
    annotationProcessor "org.projectlombok:lombok:${lombok_version}"
}